[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]

select = [
    "F",   # Pyflakes -> actual errors only
    "E9",  # Syntax errors
]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"